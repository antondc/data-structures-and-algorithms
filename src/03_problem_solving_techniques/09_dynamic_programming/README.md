# Dynamic Programming

Break a complex problem into smaller, overlapping subproblems and solve each one only once, storing their solutions in memory. Then, instead of solving the same problem multiple times, retrieve the results from memory and use them. Useful for problems where subproblems overlap.
