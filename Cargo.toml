
[lib]
name = "data_structures_and_algorithms" # The name of the target.
path = "lib.rs"

# The source file of the target.
[package]
name = "rust-exercises"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
time = { version = "0.3.9", features = ["macros"] }
cargo-watch = "8.1.1"
